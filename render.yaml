services:
  - type: web
    name: google-hackathon
    runtime: python
    buildCommand: pip install --upgrade pip && pip install --prefer-binary -r backend/requirements.txt
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT

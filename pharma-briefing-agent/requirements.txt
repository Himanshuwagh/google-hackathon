# ============================================================
# PharmaOps Sales Agent — Python Dependencies
# ============================================================

# Google ADK (Agent Development Kit)
google-adk>=0.3.0

# MCP SDK (used by the preflighted MongoDB MCP stdio wrapper)
mcp>=1.0.0

# Google Cloud
google-cloud-aiplatform>=1.60.0
google-generativeai>=0.7.0

# Google APIs (Calendar + Gmail)
google-api-python-client>=2.130.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# MongoDB — controlled briefing/status writes and db/seed_data.py
# Runtime reads use the pinned MongoDB MCP server binary.
pymongo>=4.7.0

# HTTP requests (PubMed, ClinicalTrials, OpenFDA — direct API calls)
requests>=2.31.0
httpx>=0.27.0

# Environment
python-dotenv>=1.0.1

# Utilities
pydantic>=2.7.0
